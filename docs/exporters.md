# Export Formats

Supported formats:

- JSON
- HTML
- Markdown
- Plain text
- Moodle XML
- Moodle zip package (`.qm` + media for plugin import)
- DOCX
- PDF
- Web package (HTML + CSS + JS + JSON)

Use `quizmark export` with `--format` to choose the output.
