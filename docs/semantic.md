# Semantic Rules

The validator enforces:

- At least one question
- At least two answers per question
- At least one correct answer
- No duplicate answer labels
- PASS_MARK between 0 and 100
- TIME_LIMIT positive
- Non-empty image paths
- Non-empty media values
- Valid theme keys
